<?xml version="1.0" encoding="UTF-8" ?>
<!-- <!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd"> -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.template.teacher.dao.ITeacherDao">

	<select id="queryTeacher" parameterType="long" resultType="com.mall.template.teacher.pojo.Teacher">
		select * from teacher where 1 = 1
		<if test="teacher.teacherId != null and teacher.teacherId != '' ">
			and teacherId=#{teacher.teacherId}
		</if>
		<if test="teacher.teacherName != null and teacher.teacherName != '' ">
			and teacherName like '#{teacher.teacherName}%'
		</if>
	</select>

	<insert id="saveTeacher" parameterType="com.mall.template.teacher.pojo.Teacher">
		insert into teacher ( teacherName,teacherAge,teacherType ) values
		( #{teacher.teacherName},#{teacher.teacherAge},#{teacher.teacherType} )
	</insert>
</mapper>